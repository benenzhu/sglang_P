{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Remote Attach (Rank 1)",
            "type": "debugpy",
            "request": "attach",
            "connect": {
                "host": "localhost",
                "port": 5678
            },
            "pathMappings": [
                {
                    "localRoot": "${workspaceFolder}",
                    "remoteRoot": "${workspaceFolder}"
                },
                {
                    "localRoot": "/sgl-workspace/aiter",
                    "remoteRoot": "/sgl-workspace/aiter"
                }
            ],
            "justMyCode": false
        }
    ],
    "inputs": [
        {
            "id": "remoteHost",
            "type": "promptString",
            "description": "Enter the remote host IP address",
            "default": "localhost"
        }
    ]
}
